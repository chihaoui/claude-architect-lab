// Template file for the pokeapi-component skill.
// Rename to <ComponentName>.tsx and replace placeholders before use.
// Stored as `.tsx.tmpl` so ESLint does not try to parse the placeholders.

import type { /* TYPE_NAME */ } from "@/lib/types";

interface /* COMPONENT_NAME */Props {
  readonly data: /* TYPE_NAME */;
}

export function /* COMPONENT_NAME */({ data }: /* COMPONENT_NAME */Props) {
  return (
    <div className="rounded-lg border border-zinc-200 bg-zinc-100 p-4 transition hover:-translate-y-0.5 hover:shadow-md dark:border-zinc-800 dark:bg-zinc-900">
      {/* TODO: implement */}
    </div>
  );
}
